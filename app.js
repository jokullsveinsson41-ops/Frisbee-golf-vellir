document.addEventListener("DOMContentLoaded",()=>{const reveals=[...document.querySelectorAll("[data-reveal]")];const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add("in")}),{threshold:.12});reveals.forEach(e=>io.observe(e));const search=document.querySelector("#courseSearch");if(search){const cards=[...document.querySelectorAll(".course-card")];search.addEventListener("input",()=>{const q=search.value.toLowerCase().trim();cards.forEach(c=>c.style.display=c.textContent.toLowerCase().includes(q)?"":"none")})}const tabs=[...document.querySelectorAll(".dash-tab")];const panels=[...document.querySelectorAll(".dash-panel")];if(tabs.length){const activate=(name)=>{tabs.forEach(t=>{const on=t.dataset.tab===name;t.classList.toggle("active",on);t.setAttribute("aria-selected",on?"true":"false")});panels.forEach(p=>p.classList.toggle("active",p.dataset.panel===name));history.replaceState(null,"","#"+name)};tabs.forEach(t=>t.addEventListener("click",()=>activate(t.dataset.tab)));const initial=location.hash.replace("#","");if(tabs.some(t=>t.dataset.tab===initial))activate(initial)}const share=document.querySelector("[data-share]");if(share){share.addEventListener("click",async()=>{const data={title:document.title,text:"Grafarholt frisbígolfvöllur",url:location.href};try{if(navigator.share)await navigator.share(data);else{await navigator.clipboard.writeText(location.href);share.textContent="Afritað ✓";setTimeout(()=>share.textContent="Deila",1600)}}catch(e){}})}const top=document.querySelector("[data-top]");if(top)top.addEventListener("click",()=>scrollTo({top:0,behavior:"smooth"}));});